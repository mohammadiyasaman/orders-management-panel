## توضیحات پروژه

این پروژه یک پنل داخلی برای مشاهده و بررسی سفارش‌های یک فروشگاه اینترنتی است که با React، Next.js، TypeScript و Tailwind CSS پیاده‌سازی شده است.

با توجه به اینکه Backend در زمان انجام تسک آماده نبوده، داده‌ها به‌صورت Mock در پروژه قرار گرفته‌اند و تمرکز اصلی روی پیاده‌سازی صحیح جریان داده و تجربه کاربری بوده است.

قابلیت‌های اصلی پیاده‌سازی‌شده:

* نمایش سفارش‌ها در قالب جدول
* نمایش شماره سفارش، نام مشتری، مبلغ، تعداد کالا، وضعیت و تاریخ ثبت
* ایجاد حداقل ۳۰ رکورد Mock
* جستجو بر اساس شماره سفارش و نام مشتری
* فیلتر بر اساس وضعیت سفارش
* مرتب‌سازی بر اساس مبلغ، تاریخ و نام مشتری
* Pagination با ۱۰ رکورد در هر صفحه
* نمایش جزئیات هر سفارش در Modal
* حالت Empty State در صورت نبود نتیجه
* طراحی Responsive برای Desktop و Mobile
* پشتیبانی از رابط کاربری راست‌به‌چپ و نمایش اطلاعات به زبان فارسی
* رعایت اصول پایه Accessibility در کنترل‌ها و Modal

# Architecture Decisions

## چرا این ساختار پروژه؟

ساختار پروژه به‌صورت Feature-based طراحی شده است تا کدهای مربوط به سفارش‌ها در یک محدوده مشخص قرار بگیرند.

برای مثال، منطق و کامپوننت‌های مربوط به Orders در مسیر `features/orders` قرار گرفته‌اند و به بخش‌های مستقل مانند Types، Mock Data و Components تقسیم شده‌اند.

دلیل انتخاب این ساختار، ایجاد تعادل بین خوانایی و سادگی است. با توجه به اندازه این تسک، از ایجاد لایه‌های اضافی مانند Service، Repository، State Management یا API abstraction خودداری کردم؛ چون در شرایط فعلی ارزش قابل توجهی نسبت به پیچیدگی اضافه ایجاد نمی‌کردند.

جریان پردازش داده نیز به‌صورت مشخص طراحی شده است:

`Raw Orders → Search → Filter → Sort → Pagination`

این ترتیب باعث می‌شود Pagination روی نتیجه نهایی اعمال شود و در هنگام Search یا Filter، تعداد صفحات و رکوردهای نمایش داده‌شده نیز به‌درستی به‌روزرسانی شوند.

## اگر پروژه 100 هزار سفارش داشته باشد چه تغییری می‌دهم؟

در این مقیاس، نگه‌داشتن تمام سفارش‌ها در سمت Client و انجام Search، Filter و Sort روی آن‌ها مناسب نیست.

در نسخه Production، این عملیات را به Backend منتقل می‌کنم و API را به‌صورت Server-side طراحی می‌کنم. به این شکل:

* Pagination سمت Server انجام می‌شود.
* Search و Filter به API ارسال می‌شوند.
* Sort سمت Server انجام می‌شود.
* Database برای فیلدهای پرتکرار Index خواهد داشت.
* API فقط رکوردهای موردنیاز صفحه فعلی را برمی‌گرداند.
* در صورت نیاز به نمایش حجم بسیار زیاد داده در Client، Virtualization برای جدول نیز بررسی می‌شود.

بنابراین معماری از Mock Data به API واقعی تغییر می‌کند، اما ساختار UI و Feature مربوط به Orders تا حد زیادی قابل حفظ خواهد بود.

## فرضیات و تصمیم‌های پیاده‌سازی

* در زمان انجام تسک، Backend آماده نبود؛ بنابراین از Mock Data استفاده شده است.
* برای حفظ سادگی، State مربوط به Search، Filter، Sort، Pagination و سفارش انتخاب‌شده با React State مدیریت شده است.
* به دلیل کوچک بودن Scope پروژه، از State Managementهایی مانند Redux استفاده نشده است.
* برای Pagination مقدار ثابت ۱۰ رکورد در هر صفحه در نظر گرفته شده است.
* جستجو روی شماره سفارش و نام مشتری انجام می‌شود.
* فیلتر وضعیت شامل چهار وضعیت `Pending`، `Processing`، `Completed` و `Cancelled` است.
* برای مرتب‌سازی، مبلغ به‌صورت عددی، تاریخ بر اساس Timestamp و نام مشتری بر اساس مقایسه متنی مرتب می‌شود.
* اطلاعات تاریخ در رابط کاربری با فرمت فارسی نمایش داده می‌شود.
* برای نمایش جزئیات، Modal انتخاب شده است؛ زیرا برای Scope فعلی ساده، قابل استفاده و مناسب Desktop و Mobile است.
* از Dependencyهای اضافی که برای نیاز فعلی پروژه ضروری نبودند استفاده نشده است.

## اگر Backend واقعی وجود داشت چه تغییراتی می‌دادم؟

در نسخه فعلی، داده‌ها Mock هستند و پردازش Search، Filter، Sort و Pagination در Client انجام می‌شود.

اگر Backend واقعی وجود داشته باشد، ترجیح می‌دهم این عملیات به API منتقل شوند، مخصوصاً برای داده‌های حجیم.

برای مثال API می‌تواند پارامترهایی مانند موارد زیر دریافت کند:

* `page`
* `pageSize`
* `search`
* `status`
* `sortBy`
* `sortDirection`

در این حالت Client فقط وضعیت فیلترها و صفحه فعلی را مدیریت می‌کند و داده موردنیاز را از Backend دریافت می‌کند.

همچنین در نسخه Production، مدیریت وضعیت‌های Loading، Error و Retry، اعتبارسنجی Response و مدیریت خطاهای API نیز اضافه می‌شود.

اگر نیاز پروژه ایجاب کند، می‌توان از یک ابزار مناسب برای مدیریت Server State نیز استفاده کرد؛ اما در نسخه فعلی به دلیل Mock بودن داده‌ها و کوچک بودن Scope، اضافه کردن چنین abstractionهایی را ضروری نمی‌دانستم.

## بهبودهای تکمیلی

علاوه بر موارد اصلی تسک، چند مورد را با هدف نزدیک‌تر شدن پروژه به یک UI واقعی در نظر گرفتم:

* رابط کاربری به‌صورت RTL و فارسی پیاده‌سازی شده است.
* اعداد و تاریخ‌ها با فرمت مناسب فارسی نمایش داده می‌شوند.
* کنترل‌ها و دکمه‌ها دارای حالت‌های واضح برای Hover و Focus هستند.
* Modal از نظر پایه‌ای Accessibility رعایت می‌کند و با Escape نیز قابل بسته شدن است.
* طراحی برای Mobile و Desktop در نظر گرفته شده است.

در یک پروژه بزرگ‌تر، بسته به نیاز واقعی محصول، مواردی مانند سیستم Typography و Font اختصاصی، Internationalization کامل برای چند زبان، جدا کردن formatterها و utility functionها، مدیریت متمرکزتر Server State و تست‌های واحد و Integration را نیز به ساختار اضافه می‌کردم.

اما برای این تسک عمداً از اضافه کردن abstractionهای غیرضروری خودداری کردم تا Scope پروژه کنترل‌شده باقی بماند.
